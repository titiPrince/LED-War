function pattern1(game) {

    if (Math.random() < 0.5){
    return "FILL_COLUMN"
  } else {
    return "FILL_ROW"
  }

  // else
  // if (turn % 8 === 0) {
  //   return "UP";
  // }
  // else if (turn % 4 === 0) {
  //   return "BOTTOM";
  // }
  // else if (turn % 2 === 0) {
  //   return "RIGHT";
  // }
}
