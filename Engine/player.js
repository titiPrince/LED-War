class Player {
  x = 0;
  y = 0;
  constructor(x, y, color) {
    this.x = x;
    this.y = y;
    this.color = color;
  }
  moveRight(boardState) {
    console.log("want move right");
  }
  moveLeft(boardState) {}
  moveUp(boardState) {}
  moveBottom(boardState) {}
}
